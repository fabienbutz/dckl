{
  "name": "@dckl/mcp",
  "version": "0.1.0",
  "description": "MCP server for dckl: a discipline layer over GitHub Issues with a temporal-sterile filter. STDIO transport.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "dckl-mcp": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@dckl/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
